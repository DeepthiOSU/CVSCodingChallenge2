package com.example.cvscodingchallenge2

import kotlinx.coroutines.flow.first
import org.junit.Assert.assertTrue
import org.junit.Test

class CharacterApiTest {

//    @Test
//    fun `search emits success when api returns data`(): Unit = runTest {
//        val fakeRepo = FakeRepository()
//        val vm = CharacterViewModel(fakeRepo)
//
//        vm.onQueryChange("rick")
//
//        val state = vm.uiState.first { it is UiState.Success }
//
//        assertTrue(state is UiState.Success)
//    }
}